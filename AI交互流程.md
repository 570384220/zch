# 交互流程（强制，每条消息独立判定，不可跳过）

## 0. 最高优先级

- 本规则优先于「结合历史推断为续作而直接执行」的判断。
- **同一会话中已确认过上一轮任务，不代表后续消息可跳过阶段一。**
- 收到每条用户消息后，必须先做「消息分类」（见 §1），再决定走哪个阶段；不得默认进入执行。

## 1. 每条用户消息必须先分类

收到用户消息后，**先分类、再行动**（分类可在内心完成，但结论必须正确）：

| 类型 | 判定 | 处理方式 |
|------|------|----------|
| A. 纯问答 | 只问概念、代码含义、对比、原因，不要求改代码/跑命令 | 直接回答，不走三阶段 |
| B. 新任务 | 新的功能、bug、排查、重构、运行命令、改文件等 | **必须走阶段一 → 等确认 → 阶段二 → 阶段三** |
| C. 已确认任务的续作 | 用户**仅**在上一轮阶段一已确认的步骤范围内补充细节（如「第2步改成用 Redis」） | 可跳过阶段一，直接阶段二；交付仍走阶段三 |
| D. 计划外变更 | 目标、范围、方案与已确认步骤明显不一致 | **重新走阶段一**，不得沿用旧确认 |

### 1.1 默认按「新任务 B」处理

拿不准时，**一律按新任务 B**，走阶段一。不得因「看起来像跟进」而跳过。

### 1.2 下列情况一律视为「新任务 B」（必须阶段一）

- 用户提出新的需求、bug、优化、排查，即使用词是「还有」「另外」「再帮我」
- 用户切换了功能模块、文件、接口、页面
- 上一轮任务已交付（阶段三已完成），用户的下一条 executable 消息
- 用户说「继续」但未明确指向已确认步骤中的某一步
- 距离上次阶段一确认之间，对话已转向其他话题

### 1.3 仅下列情况可视为「续作 C」（可跳过阶段一）

同时满足全部条件：

1. 上一轮**同一任务**的阶段一已被用户明确确认（确认/开始/OK/可以）
2. 用户消息**没有**改变任务目标或范围
3. 用户消息**仅**调整已列步骤的细节、参数、命名等

## 2. 阶段一：确认理解（新任务 B / 计划外变更 D）

当消息属于 B 或 D 时：

1. **禁止**调用任何工具（读文件、改文件、运行命令、搜索等）
2. **必须**先输出：

### 我理解的任务
（1～3 句话复述本轮目标）

### 执行步骤
1. 第一步：...
2. 第二步：...

### 需要您确认
请回复「确认」或「开始」后我再执行。如需调整请直接说明。

3. **必须等待**用户对**本轮**任务确认后，才能进入阶段二

### 2.1 「确认」的有效范围（重要）

- 「确认/开始/OK/可以」**只对紧接其后的那一轮任务有效**
- **不得**用历史某次「确认」授权本会话后续所有任务
- 用户说「不用确认，直接执行」→ 仅跳过**当前这条消息**的阶段一，阶段三仍必须执行

## 3. 阶段二：执行

- 按**当前任务**已确认的步骤执行
- **动手改代码前**：先搜索并阅读项目中 1～2 个同类实现，再按既有模式编写
- 执行中发现与计划偏差较大 → 暂停，重新走阶段一

## 4. 阶段三：验证与交付（每个独立任务完成后强制）

每个任务（B 或 C）完成后，**禁止直接结束**，必须先验证再交付。

### 4.1 参考实现对照（必做）

找 1～2 个同类稳定功能，对照：分层结构、接口风格、业务惯例、命名与类型。  
交付时写明参考了哪个文件、与参考的差异（若有）。

### 4.2 前后台契约比对（涉及 API/表单/导出时必做）

核对：路径与方法、请求参数、响应结构、边界行为（空数据、异步导出、错误提示等）。

### 4.3 编译与静态检查（能跑则跑）

对改动模块编译；检查 linter；条件允许时做最小接口验证。

### 4.4 交付格式
