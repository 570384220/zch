# 交互流程（强制，不可跳过）

收到需要执行的任务（写代码、改代码、排查、重构、运行命令等）时，必须严格遵守本流程，不得跳过阶段一和阶段三。

## 阶段一：确认理解（收到任务后 FIRST，禁止执行）

当用户提出需要执行的任务（写代码、改代码、排查、重构、运行命令等）时：

1. **禁止**在此阶段调用任何工具（读文件、改文件、运行命令、搜索代码等）
2. **必须**先输出以下结构：

### 我理解的任务
（用 1-3 句话复述目标）

### 执行步骤
1. 第一步：...
2. 第二步：...
3. ...

### 需要您确认
请回复「确认」或「开始」后我再执行。如需调整请直接说明。

3. **必须等待**用户明确确认（「确认」「开始」「OK」「可以」等）后，才能进入阶段二

## 阶段二：执行

- 按已确认的步骤逐步执行
- **动手改代码前**：先搜索并阅读项目中同类功能的实现（见下方「参考实现对照」），再按既有模式编写
- 若执行中发现与计划偏差较大，**暂停**并说明原因，等待再次确认

## 阶段三：验证与交付（强制，不可跳过）

全部步骤完成后，**禁止直接交付**。必须先完成验证，再给出最终答案。

### 3.1 参考实现对照（必做）

在项目中找到 **1～2 个同类、已上线或稳定运行的功能**，逐项比对本次改动是否一致：

| 对照维度 | 检查内容 |
|---------|---------|
| 分层结构 | Controller → Service → Mapper/Util 是否与同类功能一致 |
| 接口风格 | 路径命名、`@GetMapping`/`@PostMapping`、`@FormToken`、`AjaxResult`/`TableDataInfo` 返回格式 |
| 业务惯例 | 分页查询、同步/异步导出、参数校验、异常提示、开关配置（如 `SystemParamUtil`） |
| 命名与类型 | Query/VO/BO 字段名、枚举值、常量是否与现有代码统一 |

**输出要求**：在交付说明中写明「参考了哪个功能」（附文件路径），以及本次实现与参考的差异（若有）。

### 3.2 前后台契约比对（涉及接口时必做）

若改动涉及 API、导出、表单提交等前后台交互，**必须**核对前后台是否对齐：

| 对照项 | 前台侧 | 后台侧 |
|-------|--------|--------|
| 请求路径与方法 | 前端 API 调用 URL、`GET`/`POST` | Controller `@RequestMapping` + 方法注解 |
| 请求参数 | 表单字段名、Query 参数、RequestBody 结构 | Query 对象、`@RequestParam`、`@RequestBody` VO |
| 响应结构 | 列表字段、`code`/`msg`/`data` 解析 | `AjaxResult`、`TableDataInfo`、VO 字段 |
| 边界行为 | 空数据提示、超量导出跳转异步、loading/错误处理 | `countForExport` 阈值、`RuntimeException` 文案 |

**查找方式**：用接口路径或功能关键词搜索前端调用代码（如 `export`、`asyncExport`、`/api/xxx`），与 Controller/Service 逐字段比对。

### 3.3 编译与静态检查（能跑则跑）

- 对修改过的模块执行编译（如 `gradle compileJava` 或项目惯用命令）
- 检查改动文件的 linter/诊断，确保无新增错误
- 若环境允许，对关键接口做最小化调用验证（curl、已有测试类、启动后手动触发）

### 3.4 自检清单

- [ ] 是否完成全部确认步骤
- [ ] 改动是否符合任务目标
- [ ] 是否已对照至少一个同类参考实现，并说明参考来源
- [ ] 涉及接口时，前后台路径、参数、返回是否一致
- [ ] 编译/静态检查是否通过（或已说明无法执行的原因）
- [ ] 有无遗漏或引入新问题

### 3.5 交付格式

验证通过后，按以下结构交付：

### 验证结果
- 参考实现：<文件路径>（对照了 xxx 点）
- 前后台比对：<一致 / 差异说明>
- 编译/检查：<通过 / 未执行原因>

### 改动说明
- 做了什么、改了哪些文件、结果如何

- **验证未通过**：说明问题并修复，重新验证后再交付
- **无法完整验证**（如缺前端代码、无法启动服务）：明确列出未验证项及风险，不得当作已完成

## 例外（无需确认）

以下情况可直接回答，无需等待确认：
- 纯问答（概念解释、代码含义、技术对比）
- 用户消息已包含「确认」「开始」「直接做」等明确授权
- 用户明确说「不用确认，直接执行」

> 例外仅跳过「阶段一确认」，**阶段三的验证与交付仍然强制执行**。
