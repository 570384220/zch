# AI后台编码规范

## 注释规范
- 每个生成的类都要添加注释
- 类里面的属性如果是状态、类型、标识，都要把每一种可能含义的值列出来
- service接口，中每个方法上面都要有名称的方法用途，和参数注释，返回值说明
- service对应的实现类里面的方法，无论是共有方法，还是私有方法都要有方法描述的参数注释，返回值说明
- 当一个方法中调用其他的方法（公有、私有），都要添加调用方法的注释，即方法调用方法的描述也加到调用处，在调用处只添加方法描述即可；这样一眼看过去知道方法的作用，不用再点击去实际逻辑处理的里面查看描述

## 换行规范
- 这个写代码的时候需要添加必要的换行，当然不需要每一行代码都需要换行

## 生成DDL、DML规范
- 建表时，生成的实体类名不需要加上表名的开头，如BL、OPM、NL、BF等，即表名第一个下划线前面的字母不作为实体类的开头

## 旧代码改动规范
- 对于不改动的行代码，后面的注释也不要修改或者删除

## for循坏内逻辑处理规范
当你生成了针对for循环里面的查询、更新或者删除操作，要考虑能否进行先批量查询，将结果转换成Map后获取处理，之后在for逻辑中通过key来获取，避免频繁查库；对应更新也是如此要将所有结果处理成一个待更新的集合，最后统一批量更新；删除也是最后统一批量删除

## 后端项目模块依赖
graph TB
    subgraph 基础层
        R[module-repository<br/>数据访问层]
        C[module-common<br/>公共组件]
        S[module-service<br/>业务服务层]
        A[module-api<br/>共享 API/Controller]
    end

    subgraph Web 应用层 - 依赖 module-api
        MNG[module-mng<br/>金融中心端]
        CNT[module-cnt<br/>借款人客户端]
        HXCNT[module-hxcnt<br/>核心企业端]
        APPSERV[module-appserv<br/>App 服务]
    end

    subgraph Web 应用层 - 直接依赖 module-service
        PCENTER[module-pcenter<br/>个人中心]
        TASK[module-task<br/>定时任务]
        DATAAPI[module-dataapi<br/>内部数据接口]
    end

    TEMPLATE[module-template<br/>模板转换<br/>独立模块]

    R --> C --> S --> A
    A --> MNG
    A --> CNT
    A --> HXCNT
    A --> APPSERV
    S --> PCENTER
    S --> TASK
    S --> DATAAPI

## 接口修改范围影响规范
当你修改module-common、module-service、module-api中的接口时，一定要查看项目的依赖，判断被修改的接口是否也在其他模块中使用，并告知哪个模块那个页面中使用了，告知我我同意后才能修改

## 参数重复处理错误
比如，调用service中的入参，需求把其中一个参数转换成适用数据库in查询的参数，当一次请求第二次调用service方法时，可能会将处理过的参数又重复处理了一次，导致最终传入数据库发生sql异常

## 提款金额占用情况
提款状态处于12已退回、14退回补充资料时，申请的额度金额仍是占用的了你要记住

## 异常类型检查
当一个方法调用另一个方法的时候，一定要检查当前方法抛出的异常类型是否能兼容调用方法抛出的异常

## 涉及到金额的计算规范（必须遵循）
一定要谨慎处理，明确当前处理的数值是分还是元还是万，一定要关联其存入和使用的地方来确认当前的单位
